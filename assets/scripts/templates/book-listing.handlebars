{{#each books as |book|}}
  {{log book}}
<!-- {{> partial}}
  Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <h1 data-id="{{book.id}}" class="my-book"> <small>Title: {{book.title}}</small></h1>
  <ul data-id="{{book.id}}">

    <li>{{#if book.author}}
      {{book.author}}
    {{else}}
      No author provided.
    {{/if}}</li>

    <li><button type="submit">Remove</button></li>
  </ul>

{{/each}}
